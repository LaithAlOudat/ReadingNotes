### The fundamental to bulding react apps:
##### To build a react app you will use an arrow functions, classes, ES6 and, let and const statements
##### The steps you should follow to write a react app:
###### 1) write code in the browser: this is the quickest way to get started.
###### 2) Local development environment
##### what is react ? React is a declarative, efficient and flexible JS library for building user interfaces. It has a different kinds of components.
##### The smallest react example looks like this: 
###### reactDOM.render(
###### <h1>Hello, world!</h1>,
###### document.getElementById(‘root’)
###### );

### Intro to JSX:
##### what is JSX ? it’s produces React elements.
##### Why JSX ? React embrace the fact that rendering logic is inherently coupled with order UI logic: how event are handled, how the state changers over time, and how the data is prepared for display.
##### Embedding Expressions in JSX: you can put any vaild JS expression inside the curly braces in JSX.
