## Chapter 4 (Links):
#### We can create links in html with <a></a> element. We start the code with opening tag <a href=” the link that we want to navigate to it “> the name of the hyper link</a> then the closing tag.
#### Two methods to link our page to other pages. 1) link the page with other page in the same website. 2) link the page with other pages that from another site.
#### parent and child structure: parent: is a directory contains files that called childs.
#### you can link your page with an email by link it with <a></a> element.

## Chapter 15 (Layouts):
#### CSS treats each html elements as it is in its own box. This box will either be a block-level box or inline box.
#### Block-level elements like heading element, paragraph element, lists, etc.
#### inline elements like img element, mark element, emphasis element, etc.
#### containing or parent element: is a block-level element which contain other block-level element.

## Chapter (What is a function?)
#### let you group a series of statements tighter to perform a specific tasks to save your time and don’t repeat yourself over and over.
#### Declaring a function: To create a function, you give it a name and then write the statements needed to achieve its task inside the curly braces.
#### calling a function: you can call the function you already declared by write its name then ();.
